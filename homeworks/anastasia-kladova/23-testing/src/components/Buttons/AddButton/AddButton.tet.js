import { describe, it } from "node:test";
import {render} from '@testing-library/react';
import AddButton from "./AddButton";

 describe(AddButton, () => {
    it('', () => {
        const {} = render(<AddButton />)
    });
 });